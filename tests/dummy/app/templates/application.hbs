<div class="container">
  <h2>Ember Power Select Complex</h2>

  {{#power-select-complex
    selected=selectedParams
    optionsComponent="select-options"
    onchange=(action "updateParams")
    as |item|
  }}
    {{item.label}}: {{item.name}}
  {{/power-select-complex}}

  {{outlet}}
</div>
